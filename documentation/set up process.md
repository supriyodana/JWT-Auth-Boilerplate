# Prerequisites
Node.js (v18 or higher)
MongoDB (local or Atlas)
npm package manager


# Backend Setup

1. Inside backend directory install dependencies: npm install

2.  .env file set, in the backend root.

3. Start MongoDB (if running locally).

4. Run backend: npm run dev

